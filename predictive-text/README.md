## predictive-text

### repl:

Example:

```shell
    $ (exit with: !) query [percentage dictfile] 24
    No matches!
    $ (exit with: !) query [percentage dictfile] igoritas
    ['irrigators', 'largitions', 'orgiastic', 'ovaritis', 'riotings', 'trisagion', 'amortising', 'migrations', 'originates', 'instigator']
    $ (exit with: !) query [percentage dictfile] 
```
